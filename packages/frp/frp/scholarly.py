from pathlib import Path
import pandas as pd


class FRPScholarlyAnalysis:
    """
    Handles the cleaning and matching of MyCV CSVs to FRPs
    """
    def load(self, csv_location: Path) -> pd.DataFrame:
        pass
